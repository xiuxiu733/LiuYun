<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LiuYun.Views.EmojiPage"
    NavigationCacheMode="Required"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:LiuYun.Views"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <DataTemplate x:Key="EmojiCardTemplate" x:DataType="x:String">
            <Border Background="#15000000"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="4,4">
                <TextBlock Text="{x:Bind}"
                           FontSize="14"
                           FontFamily="Cascadia Mono"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"
                           TextAlignment="Center"/>
            </Border>
        </DataTemplate>

        <Style x:Key="EmojiGridItemStyle" TargetType="GridViewItem">
            <Setter Property="Margin" Value="3,0,3,4"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewItem">
                        <ContentPresenter Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          HorizontalAlignment="Stretch"
                                          VerticalAlignment="Stretch"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="CategoryTemplate" x:DataType="local:EmojiCategory">
            <StackPanel Margin="0,0,0,4">
                <TextBlock Text="{x:Bind Name}"
                           FontSize="14"
                           Margin="2,4,0,2"/>
                <GridView ItemsSource="{x:Bind Items}"
                          HorizontalAlignment="Stretch"
                          ItemTemplate="{StaticResource EmojiCardTemplate}"
                          ItemContainerStyle="{StaticResource EmojiGridItemStyle}"
                          IsItemClickEnabled="True"
                          SelectionMode="None"
                          ItemClick="KaomojiGridView_ItemClick"
                          SizeChanged="CategoryGridView_SizeChanged"
                          Padding="0"
                          ScrollViewer.VerticalScrollMode="Disabled"
                          ScrollViewer.VerticalScrollBarVisibility="Disabled"
                          ScrollViewer.HorizontalScrollMode="Disabled"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Horizontal"
                                           ItemHeight="40"/>
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>
                </GridView>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="12,8,12,4" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="颜表情"
                       FontSize="18"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Height="30"
                    Padding="10,0"
                    VerticalAlignment="Top"
                    Click="BackButton_Click"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE72B;" FontSize="12"/>
                    <TextBlock Text="返回"/>
                </StackPanel>
            </Button>
        </Grid>

        <ScrollViewer Grid.Row="1"
                      Margin="0,0,-3,0"
                      Padding="12,0,-3,12"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollMode="Enabled"
                      HorizontalScrollMode="Disabled">
            <ItemsControl ItemsSource="{x:Bind Categories}"
                          HorizontalAlignment="Stretch"
                          ItemTemplate="{StaticResource CategoryTemplate}"/>
        </ScrollViewer>
    </Grid>
</Page>
